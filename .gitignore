# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Rust (in case ops build alongside)
target/
Cargo.lock

# Local secrets and environment overrides
.env
.env.local
*.local.yaml
*.local.yml

# Logs / scratch
*.log
scratch/
tmp/

# Node (flow.qa.explore's reproduction gate needs @playwright/test installed
# in this repo when it's used as the app-under-test; keep the lockfile,
# ignore the install + generated artifacts)
node_modules/
test-results/
playwright-report/
.praxec/
